{
  "name": "pi-speckit-loop",
  "version": "0.1.0",
  "type": "module",
  "description": "Pi extension automating GitHub Spec Kit SDD with a custom Ralph Wiggum loop.",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "node --import tsx --test"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.10",
    "@types/node": "^26.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
